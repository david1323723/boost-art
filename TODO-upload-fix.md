# Upload Fix TODO

- [x] 1. Identify root cause: endpoint mismatch + missing auth
- [x] 2. Fix client endpoint in `AdminUpload.js` (`/users/posts` → `/posts`)
- [x] 3. Add admin auth to server `POST /api/posts` route in `server.js`
- [x] 4. Populate `uploadedBy` and `uploadedByName` from authenticated admin

